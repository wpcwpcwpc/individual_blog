# RESUME —— 简历上站与脱敏边界

**结论先行：简历本身不脱敏，原样公开。** 简历是主动投递给雇主的对外材料，
姓名、联系方式、雇主名称本来就公开；把公司名泛化成"某公司"反而让人看不出你做过什么。

脱敏只针对**公司业务资产**：内部产品/系统名、内部项目代号、内网地址路径、业务指标精确值。
边界定义见 `GLOSSARY.md` 第〇节。

## 一、上站形态（双形态：站点阅读 + PDF 投递）

| 形态 | 位置 | 说明 |
|---|---|---|
| **简历文本页** | `/resume` | 由 `src/data/profile.ts` 渲染；可选中、可复制、可被搜索引擎与招聘系统读取 |
| **简历 PDF 查看器** | `/resume/pdf` | pdf.js 逐页渲染 `public/resume/resume.pdf`，裸文档（不套导航与页脚），纯净阅读 |
| PDF 原件 | `public/resume/resume.pdf` | **投递与打印形态的唯一原件**；下载入口挂在文本页与查看器页 |

### 内容源与一致性

- 站点侧内容源是 `src/data/profile.ts`（首页身份区、技术栈区块、关于页、简历文本页共用它）
- 投递侧内容源是 `public/resume/resume.pdf`（人工产出，`docx → pdf`）
- **改简历 = 两处都改**：更新 `profile.ts` 与 PDF 原件，然后跑构建
- 一致性由 `npm run resume-sync`（`scripts/check-resume-sync.mjs`）强制：
  从 PDF 抽文本，断言站点侧关键事实（雇主、学校、学位、时间区间、指标精确值、邮箱）**都能在 PDF 里找到**，
  缺一条即构建失败
- 校验方向是**单向包含**（PDF ⊇ 站点侧事实）：站点刻意只展示子集，且展示区用弹性区间，
  所以"PDF 里多出内容"不算问题
- 事实清单不手写：脚本从 `profile.ts` 现抽（`--dump` 可打印），避免清单本身随内容改动漂移

### 为什么不再"以 PDF 为唯一内容源"

早期约定是"PDF 是唯一内容源，页面上不写死任何履历文本"。代价是简历在站内最不可读：
canvas 里的字不可选、不可复制、ATS 解析不到，等于把求职门面最该被读到的内容锁进图片。
现在拆成两种形态各司其职，并用构建期校验替代"记得两处同步"的人工约定。

### 路径稳定性

- 顶部导航「简历」直达 `/resume`（文本形态，信息密度最高）
- `/resume` 与 `/resume/pdf` 一经发布不得变更；只允许换页面内容，不允许形态消失（见 `showcase-resume-handoff` 规格）
- PDF 缺失时文本页完整可用，PDF 入口显示不可用提示而非指向不存在的文件

渲染实现要点（`src/pages/resume/pdf.astro`）：

- 用 **pdf.js**（`pdfjs-dist` 的 legacy 构建 + 独立 worker）逐页渲染成 canvas，
  移动端不内联 PDF 的内核（iOS Safari 等）也能直接翻页；页面按可视区懒渲染，窗口缩放后按新像素重画
- 页面是独立 HTML 文档（不套 `BaseLayout`），`<style is:global>`——canvas 由 JS 创建，
  作用域样式选不中它
- pdf.js 加载/渲染失败时**自动退回浏览器内置阅读器**（`<object>`），无 JS 环境走 `<noscript>` 里的同一兜底
- 代价：canvas 里的文字不可选中、不可复制（要文本请走 `/resume` 文本页或下载 PDF 原件）
- 换新 PDF 时注意字体是否内嵌：本页未配 `cMapUrl`，非内嵌 CJK 字体可能显示异常；
  现有 `resume.pdf` 的 7 个字体全部内嵌，无此问题

文本页排版要点（`src/pages/resume/index.astro`）：

- 语义化标签承载内容（`h2` 分节 + `ul` 列要点），不用表格堆视觉列，保证 ATS 与读屏可解析
- 身份/方向/技术栈块复用 `typography.css` 的通用规则（`.lede` / `.chips` / `.stack`），
  与首页身份区同一套排版，避免"同一份履历两种长相"

## 二、闸门对这些文件的态度（豁免 = 仍然告警）

豁免清单与说明：

| 文件 | 闸门态度 |
|---|---|
| `public/resume/resume.pdf` | **豁免**（`scripts/check-pdf.mjs`）：命中只告警不拦构建 |
| `src/pages/resume/pdf.astro` | **豁免**（`scripts/sanitize-check.mjs`）：只承载 PDF 容器，无履历文本；保留豁免以防文本回归 |
| `src/pages/resume/index.astro` | **不豁免**：文本页是站点内容，必须过同一套闸门 |
| `src/data/profile.ts` | **不豁免**：同上 |

豁免不等于不检查：命中仍然打印告警，让"哪些内部标识被公开了"始终可见，只是不拦构建。
当前告警项（本人已确认可公开）：

- 简历 PDF：内部 QA 工具链 / 设备农场平台 / 协作单系统各 1 处，外加手机号
- 简历文本页与 `profile.ts`：雇主名称实名（不属业务资产，已确认可公开）；
  内部产品名、项目代号仍按词表泛化书写

逐条判断标准：

| 告警类型 | 简历里的处理 |
|---|---|
| 手机号 / 邮箱 | PDF 保留手机号（投递材料本就给雇主）；**站点侧只放邮箱与代码托管主页，手机号不上站** |
| 姓名 / 学历 / 雇主名称 | 保留（雇主名已确认实名，站内与 PDF 口径一致） |
| 内部产品名、内部系统名 | **泛化书写**（`profile.ts` 里同样泛化，如"通用 Agent 编排框架"） |
| **内部项目代号**（游戏项目代号） | **不要出现**：未公开的项目名属业务资产，且与站内泛化口径冲突 |
| 内网域名 / 端口 / 真实路径 / 工号 | **必改**：出现在公开页面没有任何正当理由 |

## 三、口径一致性（避免面试官同时看简历与站点撞见两个数字）

两套口径各自合法，边界写死在 `GLOSSARY.md` 第三节：

- **简历文本页与 PDF** 用履历原文的**精确值**（19 个 Agent、30+ 工具、150+ Spec Change、
  170+ 用例、80% 覆盖率、2 pd、30min+、80 人日等），已在第三节逐条登记
- **站内展示区**（首页、项目页、文章、deck）用**弹性区间**（十余个 / 数十个 / 百余条 / 上百条）
- 两套口径同基线、不冲突；区间必须"框得住"精确值，改简历后若区间不再成立，两处一起改

要点是**同一个词不要指向两个对象**：站点写"回归执行耗时"（人天级 → 小时级），
简历写"评测耗时"（小时级 → 分钟级），两者是两个指标，各自写清对象即可。

## 四、发布后须知

- 简历页与 PDF 一旦公开，即便之后修改，搜索引擎与第三方聚合站的副本可能仍存在
- 每次改简历后跑一次 `npm run build`（内部依次带 `sanitize`、`figures`、`pdf-audit`、
  `resume-sync` 四道告警/硬闸门）
- 终审结论按 `RELEASE-CHECKLIST.md` 记录，便于追溯线上版本对应哪一版简历
