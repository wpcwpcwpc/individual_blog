# SHOTS —— 截图占位覆盖清单

页面上需要真实界面截图的位置，先放统一占位组件（`src/components/ShotPlaceholder.astro`），
本清单跟踪其取景建议与脱敏注意；本人脱敏后替换为图片并勾选状态。

## 当前策略：本轮不插入占位

信息量由**重绘架构图**承载（`diagrams/*.mmd`），线上不出现未完成的虚线占位框——
面试官看到"截图占位"的观感，比看到一张干净的架构图差得多。
下表登记的是**将来值得补的真实界面**，补齐后按登记替换对应位置，并勾选状态。

| 编号 | 页面 | 标题 | 取景建议 | 脱敏注意 | 状态 |
|---|---|---|---|---|---|
| S01 | `/projects/qa-agent` 编排层 | 任务路由与链路视图 | 截取一条真实任务的链路视图：左侧任务列表、右侧步骤时间线，能看到"规划 → 就绪 → 执行 → 收尾"四段 | 任务标题与用例名需替换为演示数据；裁掉账号头像与用户名 | `- [ ]` |
| S02 | `/projects/qa-agent` 管控层 | 越权拦截现场 | 截取一次被拦截的工具调用：调用、拦截提示、模型按提示改用正确用法的三段连续输出 | 拦截提示中若有内部服务名需遮挡；不要露出终端里的真实路径 | `- [ ]` |
| S03 | `/projects/qa-agent` 评测层 | 分数卡与基线对比 | 截取一张分数卡：三层指标与"与上一版对比"的差值 | 指标数值需替换为区间演示值；裁掉改动标识与协作单号 | `- [ ]` |

## 使用说明

1. 内容侧插入占位：

   ```astro
   <ShotPlaceholder id="S01" title="某界面视图" hint="建议取景：…" />
   ```

   注意：Markdown 内容无法直接使用 Astro 组件；如需在文章中放占位，
   由页面模板插入，或改用 `.mdx`。

2. 取景前先读本表「脱敏注意」列，确认以下内容不出现在画面中：
   - 内部系统名、内网地址与端口
   - 真实人员姓名、头像、工号
   - 内部工单号、知识库链接
   - 精确的业务数字（如需保留，做区间化或模糊处理）

3. 替换后把状态改为 `- [x]`，并在 RELEASE-CHECKLIST.md 的终审记录中留痕。
