# GLOSSARY —— 脱敏边界与口径登记

> **本文件 MUST NOT 删除**：第三节是 `npm run figures` 的口径来源，删了构建必挂。
> 它已不含任何内部标识 —— 要藏的词表不在这里（见下）。

> **禁出词表与泛化映射表不在本仓库。** 公开仓库 MUST NOT 登记任何内部标识 ——
> 早期版本把词表放在这里，等于把"内部名 → 对外泛称"的对照关系整体公开，
> 泛化沦为摆设。现改为：本地 `GLOSSARY.local.md`（已 gitignore）+ CI secret `GLOSSARY_WORDS`。
> 本文件只保留可公开的两部分：脱敏边界（第〇节）与指标区间化登记（第三节）。
> 本文件 MUST NOT 复制进 `public/` 或进入任何部署产物（`RELEASE-CHECKLIST.md` 第一节核对）。

---

## 〇、脱敏边界（先明确"什么要脱敏"）

**硬红线是"让人看到公司的东西 / 连上公司的系统"**，不是"提到了内部名词"：

| 类别 | 处理 | 说明 |
|---|---|---|
| 游戏实际内容（玩法、数值、未公开设定与截图） | **禁止出现** | 产品资产，改写不足以消除，只能不写 |
| 公司战略与规划（未公开的路线、组织、商业数据） | **禁止出现** | 同上 |
| 鉴权信息（账号、口令、Token、密钥、证书、Cookie） | **禁止出现** | 泄露即可被利用，无泛化余地 |
| API 信息（内部接口地址、契约、请求/响应样例、密钥） | **禁止出现** | 词表里的正则兜内网域名与 IP:端口，其余靠人工终审 |
| 内网域名 / 端口 / IP / 真实文件路径 / 工号 | **禁止出现** | 见词表 2.2 正则模式 |
| 内部产品 / 平台 / 系统名 | **泛化** | 见词表第一节映射表 |
| 内部项目代号（游戏项目、代号） | **泛化** | 未公开的项目名属业务资产 |
| 业务指标精确值 | **区间化** | 见第三节 |
| 个人身份（姓名、GitHub、公开邮箱） | **可公开** | 对外身份，站点与简历都用 |
| 雇主公开名称、行业域（游戏 QA 等） | **可公开** | 简历里本来就写，站点不必回避——过度泛化会让读者看不出你在做什么 |
| 源码类内部事实（内网地址与端口、凭证位、业务数据模型、内部服务契约） | **禁止出现** | 源码搬运时按删除 / 泛化 / 占位三选一处置，见 `openspec/changes/add-qa-agent-source` 的 `source-sanitization` 规格 |
| 源码镜像区（`mirror/`） | **只删不创、不上站** | 不进构建输入与产物、不被页面引用；逐文件处置清单（含真实文件名）MUST NOT 入库，见 `SANITIZE-LIST.local.md` |

> 判断口诀：**这条信息能让人"看到公司的东西"或"连上公司的系统"吗？**
> 能，就脱敏（前五类禁出，内部名词泛化）；不能，照写。

---

## 一、禁出词表 —— 不入公开仓库

两个扫描器（`scripts/sanitize-check.mjs`、`scripts/check-pdf.mjs`）按此顺序取词表：

1. 环境变量 `GLOSSARY_WORDS` —— CI 用仓库 secret 注入，值是词表**全文**（markdown）
2. 仓库根的 `GLOSSARY.local.md` —— 本地开发用，已在 `.gitignore` 内

两处都取不到即失败：不允许"词表缺失就当没命中"通过。

词表形态（完整范例见 `GLOSSARY.local.md`，本仓库不入库）：

| 结构 | 解析规则 |
|---|---|
| `## 二、禁出词表` | 切分锚点，MUST NOT 改写标题 |
| `### 2.1 精确词` | ``` 代码块内的行；子串匹配，大小写不敏感 |
| `### 2.2 正则模式` | 表格首列的 `` `模式` ``；表格内的 `\|` 还原为正则或运算 |

新增内部标识的顺序：**先登记本地词表 → 再同步 GitHub secret → 最后才写作**。
只改本地不同步 CI，等于 CI 上无人拦截；只改 CI 不同步本地，本地构建会假通过。

**CI 日志同样是公开的**：命中词在 CI 里打码输出（只留前两字与字数），
本地需要看全文时设 `SANITIZE_REVEAL=1`。否则闸门一响，日志就把要藏的词写出来了。

---

## 三、指标区间化登记（口径单一来源）

**本节是全站量化事实的唯一定义处。** 对外出现的每个指标 MUST 在此登记其区间或倍数表述；
内容中 MUST NOT 出现精确内部数字。项目页 frontmatter 的 `figures` 只存放"页面上展示什么"，
其取值由 `npm run figures` 强制与本节逐字一致 —— 改一处不同步另一处，构建即失败。

「来源说明」写统计口径（数据从哪里数出来的），MUST NOT 写内部文件路径或函数名。

| 指标项 | 对外表述 | 来源说明 |
|---|---|---|
| 专职 Agent 数量 | 十余个 | 按内置 Agent 注册表中实际注册生效的条目计数，不含按需热加载的自定义 Agent |
| 内置工具数量 | 数十个 | 按工具注册表内内置工具条目计数，不含按技能与外部能力动态注册的工具 |
| 管控 Hook 数量 | 近二十道 | 按挂载到运行链路上的 Hook 模块计数，不含其依赖的工具函数与辅助模块 |
| 流水线步骤数 | 四步 | 按自动化主链路上实际编排的步骤计数：规划 → 环境就绪 → 执行 → 收尾 |
| 能力规格数量 | 四十余项 | 按规格库中已建档的能力规格计数 |
| 工程变更记录量 | 百余条 | 按变更记录库中已归档的变更条目计数，随项目演进持续增加 |
| 回归执行耗时 | 从人天级降到小时级 | 对象是**一轮回归的执行耗时**（人工串联 → 平台执行）；与"评测耗时（小时级→分钟级）"是两个不同指标，不可混用 |
| 累计沉淀用例量 | 上百条 | 已沉淀场景与用例总量；"上百条"与简历里的 170+ 兼容 |
| 长时任务连续运行时长 | 数小时级 | 无人值守长链路任务的连续运行时长区间 |
| 履历原文精确值（简历文本页与 PDF 通用） | 19 个 Agent / 30+ 工具 / 150+ Spec Change / 170+ 用例 / 100+ 场景 / 40+ 自动化场景 | 出处：本人简历原文。适用面：简历文本页与简历 PDF 属对外投递材料，直接用精确值；站内展示区（首页、项目页、文章）一律用弹性区间（十余个 / 数十个 / 百余条 / 上百条）。两套口径同基线、各自合法、不冲突 |
| 业务自动化覆盖率 | 超 80% | 简历原文 |
| 数字员工日均请求量 | 50+ 次/日 | 简历原文 |
| 日均节省人力 | 约 2 pd | 简历原文 |
| 评测耗时 | 小时级 → 分钟级 | 简历原文；对象是**评测本身**，与"回归执行耗时"是两个指标 |
| 长链路异常定位耗时 | 由 30min+ 降至面板分钟级 | 简历原文 |
| 平台研发节省人力 | 约 80 人日/月 | 简历原文（前一段经历的项目） |
| 专利与论文 | 发明专利 2 项；SCI Q2 期刊 1 篇；C 类会议 3 篇 | 简历原文 |

> 未登记的指标不得出现在内容中；新增指标先登记后写作。
> 登记值之间 MUST NOT 互相矛盾（如"十余个 Agent"与"40+ Agent"不可并存）。
>
> **两套口径的分工**：站内展示区（首页、项目页、文章、deck）用区间表述；
> **简历文本页与简历 PDF 用精确值**（对外投递材料，精确值才有说服力）。
> 同一指标在两处的取值来自同一份事实（`src/data/profile.ts` / 简历原文），
> 因此区间必须"框得住"精确值——改简历后若区间不再成立，两处一起改。
> 源码镜像区（`mirror/`）MUST NOT 引入任何对外量化表述（含文件数、规模倍数）；确需引用时先在本节登记。

---

## 四、已确认项与待定项（需人工确认口径）

- 游戏项目代号一律泛化（业务资产）；行业域"游戏 QA"可直接书写（已确认）。
- 雇主名称**已确认实名**：不属业务资产，站内可公开书写，与简历 PDF 口径一致。
  词表侧（本地 `GLOSSARY.local.md` + CI secret）已移除雇主名与关联写法；
  内网域名/邮箱后缀仍由词表 2.2 的正则拦截，实名不等于放开内部地址。
- 站内联系方式只放邮箱与代码托管主页，手机号不上站（第〇节表格同款口径）。
- 效率类指标的区间表述与简历里的精确值需保持不冲突（见 `RESUME.md` 第三节与本节末段分工说明）。
- 含内部标识的待定项（如内部产品名是否算标识）随词表一并移出公开仓库，见 `GLOSSARY.local.md`。
