# 依赖
node_modules/

# 构建产物
dist/
.astro/
src/assets/diagrams/
# 注：public/og/default.png 刻意入库——CI 缺中文字体时它作为兜底分享图照常发布

# 临时产物（核对截图、CDP profile 等，约定以下划线开头）
_cdp-profile/
_*.png

# 环境与凭据
.env
.env.*
!.env.example

# 禁出词表：公开仓库 MUST NOT 登记内部标识
# 本地词表放这里，CI 侧由仓库 secret GLOSSARY_WORDS 提供，两处内容保持一致
GLOSSARY.local.md

# 源码搬运处置清单：含真实文件名与业务点定位，等于内部模块地图，MUST NOT 入库
# 模板与用法见 openspec/changes/add-qa-agent-source/design.md D6
SANITIZE-LIST.local.md

# 源码搬运台账（add-qa-agent-source 的 tasks.md）：同属"含真实文件名的处置清单"，
# 按其规格 source-sanitization 的要求只留在本地；文件名不变是为了 openspec CLI 仍能追踪任务。
# 扫描器侧同步列入 scripts/sanitize-check.mjs 的 IGNORE（双保险：忽略条目丢失时仍不会被扫）
openspec/changes/add-qa-agent-source/tasks.md

# 源码镜像区（mirror/）：已于 9/20 迁出至独立仓库维护
# （作者拍板「移出，新库成唯一真源」，见 openspec/changes/add-qa-agent-source/tasks.md 10.1）。
# 本仓库不再保留副本；忽略条目予以恢复，防止将来误把游离副本提交进本站仓库。
mirror/

# 编辑器与系统
.DS_Store
Thumbs.db
.vscode/*
!.vscode/extensions.json
.idea/

# 日志
*.log
npm-debug.log*
